import 'package:flutter/material.dart';

class AppColors {
  // Main app colors based on pastel blue theme
  static const Color primary = Color(0xFF64B5F6);       // Pastel blue
  static const Color primaryLight = Color(0xFFE3F2FD);  // Light pastel blue
  static const Color primaryDark = Color(0xFF1E88E5);   // Darker blue for emphasis
  
  static const Color accent = Color(0xFF26C6DA);        // Teal accent
  static const Color success = Color(0xFF66BB6A);       // Green for success states
  
  // Text colors
  static const Color textDark = Color(0xFF424242);      // Almost black for main text
  static const Color textLight = Color(0xFF757575);     // Gray for secondary text
  
  // Background gradient colors
  static const Color gradientStart = Color(0xFFE3F2FD); // Very light blue
  static const Color gradientEnd = Color(0xFFBBDEFB);   // Slightly darker light blue
}